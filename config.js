// ============================================================================
// ATLAS — config.js
// Placeholder credentials. Replace with your Supabase project values.
// TODO (deploy): migrate these to Netlify environment variables and inject at
// deploy time (e.g. a build-less sed step or Netlify snippet injection). The
// anon key is public by design — RLS is the security boundary — but keeping it
// in env vars makes rotation painless.
// ============================================================================
export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-SUPABASE-ANON-KEY';

export const APP_NAME = 'ATLAS';
export const APP_TAGLINE = 'Performance Management';
